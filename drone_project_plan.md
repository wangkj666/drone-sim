# 无人机自主栖息与抓取 — 项目规划文档

## 项目概述

**目标**：实现无人机从识别目标、自主飞行接近、到栖息抓取的全流程自主任务。

**仿真平台**：NVIDIA Isaac Sim 4.5 + Python 3.10 + PyTorch 2.13

**硬件**：RTX 4060 Laptop GPU (8GB VRAM), Intel i9-14900HX, 32GB RAM

---

## 一、整体架构

```
┌──────────────────────────────────────────────────┐
│                感知层 (Perception)                  │
│  相机图像 → 目标检测 → 位姿估计 → 抓取点计算         │
├──────────────────────────────────────────────────┤
│                规划层 (Planning)                   │
│  当前位置+目标位姿 → 轨迹生成 → 路径点序列            │
├──────────────────────────────────────────────────┤
│                控制层 (Control)                    │
│  路径点 → 位置/姿态控制器 → 混控器 → 电机推力         │
├──────────────────────────────────────────────────┤
│              仿真层 (Isaac Sim)                    │
│  无人机模型 + 传感器 + 目标物体 + PhysX物理引擎       │
└──────────────────────────────────────────────────┘
```

### 数据流

```
[相机RGB-D] ──→ [目标检测] ──→ [位姿估计] ──→ [抓取规划]
                                                  │
[当前位置] ←── [IMU/里程计] ←── [状态估计] ←────────┘
     │
     └──→ [轨迹规划] ──→ [PID/MPC控制] ──→ [电机推力] ──→ [物理仿真]
```

### 任务阶段

```
阶段0: 起飞        → 上升到搜索高度 (~3m)
阶段1: 搜索        → 旋转扫描，检测目标
阶段2: 接近        → 飞向目标上方/前方 (~0.5m)
阶段3: 栖息        → 减速下降至目标表面
阶段4: 抓取        → 机械爪执行抓取动作
阶段5: 返航        → 抓取后提升→返回起点
```

---

## 二、核心算法

### 2.1 感知模块

| 子任务 | 算法方案 | 输入 | 输出 |
|--------|---------|------|------|
| 目标检测 | YOLOv8/v11 + RGB相机 | 640x480 RGB图像 | 目标类别 + 2D包围框 |
| 深度估计 | RGB-D相机 / 立体深度 | RGB + Depth | 目标3D位置 (x,y,z) |
| 6D位姿估计 | FoundationPose / DOPE | RGB-D + 目标CAD模型 | 目标姿态 (R,t) |
| 抓取点生成 | GraspNet / Contact-GraspNet | 物体点云 | N个抓取姿态候选 |

#### 简化版（快速验证）

| 子任务 | 简化方案 | 效果 |
|--------|---------|------|
| 目标定位 | ArUco/AprilTag标记 | 精确6DoF，无需训练 |
| 深度 | Isaac Sim内置深度传感器 | 免配准 |
| 抓取点 | 预定义抓取姿态 (规则物体) | 对圆柱/长方体有效 |

### 2.2 规划模块

#### 全局路径规划

| 算法 | 适用场景 | 特点 |
|------|---------|------|
| A* | 已知障碍物地图 | 最优路径，需要建图 |
| RRT* | 高维空间 | 无需显式建图，概率完备 |
| 势场法 | 动态避障 | 实时性好，可能陷入局部极小 |

#### 局部轨迹生成

| 方法 | 描述 |
|------|------|
| 贝塞尔曲线 | 平滑插值，端点约束 |
| 最小Snap轨迹 | 无人机专用，能量最优 |
| B样条 | 局部调整灵活 |

#### 阶段轨迹设计

```
搜索阶段:  盘旋扫描，半径1.5m，高度3m
接近阶段:  直线下降至目标斜上方45°，距离0.8m
栖息阶段:  垂直慢速下降，0.2m/s，至0.05m上方
抓取阶段:  保持悬停，执行器动作
```

### 2.3 控制模块

#### 级联PID控制架构

```
期望位置(x,y,z) ──→ [外环位置PID] ──→ 期望姿态(roll,pitch,thrust)
                                         │
期望偏航(yaw)   ──→ [内环姿态PID] ──→ 期望力矩(τx,τy,τz)
                                         │
                                         └──→ [混控器] ──→ 四个电机转速
```

#### 控制律公式

**位置控制（外环）**：
```
ax = Kp_x*(x_des - x) + Kd_x*(vx_des - vx) + Ki_x*∫(x_des - x)dt
ay = Kp_y*(y_des - y) + Kd_y*(vy_des - vy) + Ki_y*∫(y_des - y)dt
az = Kp_z*(z_des - z) + Kd_z*(vz_des - vz) + Ki_z*∫(z_des - z)dt
```

**姿态解算**：
```
thrust  = m * sqrt(ax² + ay² + (az+g)²)
roll_d  = atan2(ay, az+g)
pitch_d = atan2(-ax, sqrt(ay²+(az+g)²))
```

#### 可选进阶方案

| 算法 | 优势 |
|------|------|
| 模型预测控制 (MPC) | 处理约束、预判未来状态 |
| 滑模控制 (SMC) | 抗扰动能力强 |
| 几何控制 | 大角度机动，无奇异 |

### 2.4 混控器（X型四旋翼）

```
电机布局 (俯视图):
    M1(前右)    M2(前左)
        \        /
         机身中心
        /        \
    M4(后左)    M3(后右)
```

混控矩阵：

```
[M1]   [ 1  1  1 -1 ]   [thrust  ]
[M2] = [ 1 -1  1  1 ] * [roll_moment]
[M3]   [ 1 -1 -1 -1 ]   [pitch_moment]
[M4]   [ 1  1 -1  1 ]   [yaw_moment  ]
```

---

## 三、技术路线

### 路线A：快速验证（推荐先走）

```
Week 1-2: 基础仿真环境
  ├── 无人机模型 (USD/基础几何)
  ├── IMU + RGB + Depth 传感器
  ├── 级联PID控制器
  └── 手动/代码飞行

Week 2-3: 自主悬停 + ArUco检测
  ├── 场景中放置标记目标
  ├── ArUco检测 → 3D位姿
  ├── 自动悬停到指定点
  └── 路径点导航

Week 3-4: 栖息 + 抓取
  ├── 四阶段自动流程
  ├── 接近 → 下降 → 抓取
  └── 返航/放下目标
```

### 路线B：完整方案（论文级别）

```
Month 1: 基础仿真 + PID控制
Month 2: YOLO检测 + 6D位姿估计 + A*规划
Month 3: MPC控制 + GraspNet抓取 + 动态避障
```

---

## 四、关键文件规划

```
G:\isaac\drone_project\
│
├── drone_model.py           # 无人机模型（几何/URDF/USD）
├── flight_controller.py     # PID/MPC飞行控制器
├── mixer.py                 # 四旋翼混控器
├── trajectory_planner.py    # 轨迹规划（A*、贝塞尔）
├── perception.py            # 相机读取 + ArUco/YOLO检测
├── state_estimator.py       # 里程计 + IMU融合
├── gripper.py               # 机械爪模型与控制
│
├── config.yaml              # 参数配置（PID增益、无人机参数）
│
├── scripts/
│   ├── 01_fly_manual.py       # 手动控制飞行测试
│   ├── 02_hover_test.py       # 自主悬停测试
│   ├── 03_aruco_detect.py     # ArUco检测 + 定点飞行
│   ├── 04_auto_approach.py    # 自动接近目标
│   ├── 05_perch_grasp.py      # 完整栖息抓取流程
│   └── utils.py               # 工具函数
│
└── README.md                # 项目说明
```

---

## 五、传感器配置

| 传感器 | 类型 | 参数 | 用途 |
|--------|------|------|------|
| RGB相机 | `omni.isaac.sensor` | 640x480, 30Hz, FOV 90° | 目标检测 |
| 深度相机 | Isaac Sim Depth Sensor | 640x480, 30Hz | 3D定位 |
| IMU | Isaac Sim IMU | 100Hz | 加速度+角速度 |
| 碰撞传感器 | PhysX Contact Sensor | - | 栖息/抓取检测 |

---

## 六、目标场景设计

```
场景元素:
  ├── 地面 (平原 / 带障碍)
  ├── 目标物体 (圆柱/长方体, 贴ArUco)
  ├── 栖息表面 (桌面/平台, 高1m)
  ├── 障碍物 (可选, 测试避障)
  └── 光照 (默认太阳光 + 可调)

坐标约定:
  - 世界原点在地面
  - Z轴向上, Y轴前方
  - 无人机初始位置 (0, 0, 0.5)
```

---

## 七、评估指标

| 指标 | 定义 | 目标值 |
|------|------|--------|
| 悬停精度 | 位置RMSE | < 0.05m |
| 检测准确率 | 目标检测mAP | > 90% |
| 位姿误差 | 6Dof位姿估计 | 平移 < 0.02m, 旋转 < 5° |
| 抓取成功率 | 抓取实验成功率 | > 80% |
| 全程耗时 | 从起飞到抓取 | < 30s |

---

## 八、已知注意事项

### Isaac Sim 环境

```
- Python必须用3.10（不可用3.13）
- import torch 必须在 from isaacsim import SimulationApp 之前
- 运行前设置: $env:OMNI_KIT_ACCEPT_EULA = "YES"
- 激活环境: .\isaacsim_env\Scripts\Activate.ps1
- 首次启动会下载800+扩展文件（只一次）
- GPU显存8GB，复杂场景需注意优化
```

### 无人机飞行注意事项

```
- Isaac Sim 4.5中 RigidPrim需要绑定到XForm路径才能正确初始化物理
- 旋翼推力通过apply_forces或直接控制速度实现
- 传感器数据在world.step()后读取
- 时间步长默认1/60s，物理子步可调
```

---

## 九、参考资料

| 资源 | 链接 |
|------|------|
| Isaac Sim 文档 | docs.omniverse.nvidia.com/isaacsim/ |
| Isaac Lab (RL框架) | github.com/NVIDIA-Omniverse/IsaacLab |
| 四旋翼控制理论 | "Geometric Tracking Control of a Quadrotor" |
| ArUco检测 | OpenCV ArUco Module |
| GraspNet | graspnet.net |

---

> 文档版本: v1.0 | 日期: 2026-07-23 | 状态: 规划阶段
